import React from "react";
import type { Meal, MealDetails } from "../types";

type Props = {
  data: MealDetails;
};

function RecipeModalContent({ data }: Props) {
  return <div>RecipeModalContent</div>;
}
export default RecipeModalContent;
